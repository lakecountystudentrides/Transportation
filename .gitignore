# Secrets — never commit these
.env
.env.local
.env.*.local
*.pem
*.key

# Dependencies / build output (added as the site's stack is chosen)
node_modules/
dist/
.wrangler/

# OS/editor cruft
.DS_Store
